
<%= render 'layouts/flash' %>

<%= form_with model: [:admin, @theme], local: true, url: themes_path do |f| %>
    <%= f.label :name, "nom du nouveau thème" %>
    <%= f.text_field :name, :required => 'required' %>
    <%= f.submit "Ajouter" %>
<% end %>

