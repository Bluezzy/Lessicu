
<%= render 'layouts/flash' %>

<%= form_with model: [:admin, @theme], local: true, url: theme_path(@theme.id), method: :put  do |f| %>
    <%= f.label :name, "modifier nom du thème" %>
    <%= f.text_field :name,  :required => 'required' %>
    <%= f.submit "Modifier" %>
<% end %>

