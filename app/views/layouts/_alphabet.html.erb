<div class='alphabet_wrapper'>
    <ul class="alphabet">
        <% alphabet.each do |letter| %>
            <li class="letter">
                <%= link_to letter %>
            </li>
        <% end %>
    </ul>
</div>
