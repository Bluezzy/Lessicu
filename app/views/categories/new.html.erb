<header>
    <%= render 'layouts/logo' %>
    <%= render 'layouts/adminnav' %>
</header>
<%= render 'layouts/flash' %>

<%= form_with model: [:admin, @category], local: true, url: categories_path do |f| %>
    <%= f.label :name, "nom de la nouvelle catégorie" %>
    <%= f.text_field :name, :required => 'required' %>
    <%= f.submit "Ajouter" %>
<% end %>
