<header>
    <%= render 'layouts/logo' %>
    <%= render 'layouts/adminnav' %>
</header>
<%= render 'layouts/flash' %>

<%= form_with model: [:admin, @category], local: true, url: category_path(@category.id), method: :put  do |f| %>
    <%= f.label :name, "modifier nom de la catégorie" %>
    <%= f.text_field :name,  :required => 'required' %>
    <%= f.submit "Modifier" %>
<% end %>